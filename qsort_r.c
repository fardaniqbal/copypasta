/* Public domain qsort_r implementation.  Like ISO C qsort, but compare
   function takes an additional argument for context. */
#include <stdlib.h>
#include <stdio.h>

/*
void qsort_r(void *base, size_t nmemb, size_t size,
             int (*compar)(const void *, const void *, void *),
             void *arg);
*/

int
main(void)
{
  /* TODO */
  return 0;
}
